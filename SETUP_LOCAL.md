# 🚀 Локальная разработка БЕЗ Docker

## Шаг 1: Установите Python
Убедитесь, что у вас установлен Python 3.11+ (проверьте: `python --version`)

## Шаг 2: Создайте виртуальное окружение

```bash
# Перейдите в папку backend
cd backend

# Создайте виртуальное окружение
python -m venv venv

# Активируйте его
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate
```

## Шаг 3: Установите зависимости

```bash
pip install -r requirements.txt
```

## Шаг 4: Настройте API ключи

1. Скопируйте `.env.example` в `.env`:
   ```bash
   copy .env.example .env    # Windows
   cp .env.example .env      # Linux/Mac
   ```

2. Откройте `.env` и вставьте ваши ключи от iLovePDF:
   ```
   ILOVEPDF_PUBLIC_KEY=project_public_xxxxxxxxxxxxxxxx
   ILOVEPDF_SECRET_KEY=secret_key_xxxxxxxxxxxxxxxx
   ```

## Шаг 5: Запустите сервер

```bash
python run_local.py
```

Сервер запустится на `http://localhost:8000`

## Шаг 6: Протестируйте

Откройте в браузере `frontend/index.html` (просто двойной клик по файлу)
или используйте Live Server в VS Code.

В `frontend/index.html` временно измените URL на:
```javascript
const backendUrl = 'http://localhost:8000/compress';
```

---

## ✅ Готово! Теперь можно тестировать локально.

Когда закончите работу:
```bash
# Деактивируйте виртуальное окружение
deactivate
```

