# 🐙 Добавление проекта на GitHub (пошагово)

## Вариант 1: Через GitHub Desktop (проще для новичков)

### Шаг 1: Установите GitHub Desktop
1. Скачайте: https://desktop.github.com/
2. Установите и запустите
3. Войдите в свой GitHub аккаунт

### Шаг 2: Создайте репозиторий
1. File → Add Local Repository
2. Выберите папку `mega-pdf-compressor`
3. Если появится ошибка "not a git repository" — нажмите **"Create a repository"**
4. Заполните:
   - **Name:** mega-pdf-compressor
   - **Description:** PDF compressor for MEGA cloud storage
   - **Git Ignore:** Python
   - ✅ Initialize this repository with a README (можно снять галочку, у нас уже есть README)
5. Нажмите **"Create Repository"**

### Шаг 3: Опубликуйте на GitHub
1. Нажмите **"Publish repository"** (правый верхний угол)
2. Выберите:
   - **Name:** mega-pdf-compressor
   - **Description:** PDF compressor for MEGA cloud storage
   - ☐ Keep this code private (снимите галочку для публичного репозитория)
3. Нажмите **"Publish Repository"**

### Шаг 4: Готово! 🎉
Ваш проект теперь на GitHub!

---

## Вариант 2: Через командную строку (для опытных)

### Шаг 1: Проверьте, установлен ли Git

```bash
git --version
```

Если нет — скачайте: https://git-scm.com/downloads

### Шаг 2: Настройте Git (если первый раз)

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш-email@example.com"
```

### Шаг 3: Инициализируйте Git в проекте

```bash
# Перейдите в папку проекта
cd e:\Code\GitHub\mega-pdf-compressor

# Инициализируйте Git
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: MEGA PDF Compressor with iLovePDF API"
```

### Шаг 4: Создайте репозиторий на GitHub

1. Откройте https://github.com/new
2. Заполните:
   - **Repository name:** mega-pdf-compressor
   - **Description:** PDF compressor for MEGA cloud storage
   - **Public** (или Private, если хотите приватный)
   - ☐ НЕ добавляйте README, .gitignore, license (у нас уже есть)
3. Нажмите **"Create repository"**

### Шаг 5: Подключите локальный репозиторий к GitHub

GitHub покажет команды, выполните их:

```bash
# Добавьте remote (замените YOUR_USERNAME на ваш логин GitHub)
git remote add origin https://github.com/YOUR_USERNAME/mega-pdf-compressor.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

### Шаг 6: Готово! 🎉

Обновите страницу на GitHub — ваш код там!

---

## 🔍 Проверка .gitignore

Убедитесь, что `.gitignore` правильно настроен (у вас уже есть этот файл):

```
.env
__pycache__/
*.pyc
venv/
*.log
.DS_Store
```

Это важно, чтобы **НЕ** закоммитить:
- ❌ `.env` файл с секретными ключами
- ❌ `venv/` виртуальное окружение
- ❌ `__pycache__/` временные файлы Python

---

## 📦 Что будет на GitHub

После публикации на GitHub будут эти файлы:

```
mega-pdf-compressor/
├── backend/
│   ├── app.py
│   ├── Dockerfile
│   ├── requirements.txt
│   ├── run_local.py
│   └── .env.example          ✅ Шаблон (без секретов)
├── frontend/
│   └── index.html
├── .gitignore                ✅ Важно!
├── README.md
├── SETUP_LOCAL.md
├── SETUP_DOCKER.md
├── DEPLOY_RAILWAY.md
├── RAILWAY_REGISTRATION.md
├── GITHUB_SETUP.md
└── QUICK_START.md
```

**НЕ будет:**
- ❌ `.env` (секретные ключи)
- ❌ `venv/` (виртуальное окружение)
- ❌ `__pycache__/` (временные файлы)

---

## 🔐 Безопасность

### ⚠️ ВАЖНО: Проверьте перед публикацией!

```bash
# Посмотрите, что будет закоммичено
git status

# Убедитесь, что .env НЕ в списке!
```

Если случайно добавили `.env`:

```bash
# Удалите из индекса (файл останется локально)
git rm --cached backend/.env

# Закоммитьте изменения
git commit -m "Remove .env from git"
```

---

## 🚀 Что дальше?

После публикации на GitHub:

1. ✅ Зарегистрируйтесь на Railway → [RAILWAY_REGISTRATION.md](RAILWAY_REGISTRATION.md)
2. ✅ Задеплойте проект → [DEPLOY_RAILWAY.md](DEPLOY_RAILWAY.md)

---

## ❓ Частые вопросы

### Нужно ли делать репозиторий публичным?
- **Да**, если хотите использовать GitHub Pages для frontend
- **Нет**, если планируете хостить frontend отдельно

### Что если я случайно закоммитил .env с секретами?
1. Удалите файл из Git (см. выше)
2. **ОБЯЗАТЕЛЬНО** смените API ключи на iLovePDF!
3. Создайте новые ключи в личном кабинете iLovePDF

### Можно ли изменить название репозитория потом?
- **Да**, в настройках репозитория на GitHub
- Settings → Repository name → Rename

---

## 🆘 Нужна помощь?

Если что-то не получается — напишите мне, помогу разобраться!

