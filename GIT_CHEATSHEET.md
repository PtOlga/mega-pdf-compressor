# 🐙 Git Шпаргалка

## ✅ Ваш проект уже на GitHub!

Репозиторий: https://github.com/PtOlga/mega-pdf-compressor

---

## 📝 Основные команды для работы

### Проверить статус (какие файлы изменены)
```bash
git status
```

### Добавить изменённые файлы
```bash
# Добавить все изменения
git add .

# Добавить конкретный файл
git add backend/app.py
```

### Создать коммит (сохранить изменения)
```bash
git commit -m "Описание изменений"
```

Примеры хороших сообщений:
- `"Добавил выбор уровня сжатия в интерфейс"`
- `"Исправил ошибку с CORS"`
- `"Обновил README с инструкциями"`

### Отправить изменения на GitHub
```bash
git push
```

---

## 🔄 Типичный рабочий процесс

```bash
# 1. Внесли изменения в код
# 2. Проверяем, что изменилось
git status

# 3. Добавляем изменения
git add .

# 4. Создаём коммит
git commit -m "Описание того, что сделали"

# 5. Отправляем на GitHub
git push
```

---

## 🔍 Полезные команды

### Посмотреть историю коммитов
```bash
git log --oneline
```

### Посмотреть изменения в файлах
```bash
# Все изменения
git diff

# Изменения в конкретном файле
git diff backend/app.py
```

### Отменить изменения в файле (до коммита)
```bash
git checkout -- backend/app.py
```

### Посмотреть удалённые репозитории
```bash
git remote -v
```

---

## 🚨 Важные правила

### ❌ НИКОГДА не коммитьте:
- `.env` файлы с секретными ключами
- `venv/` виртуальное окружение
- `__pycache__/` временные файлы
- Пароли, токены, API ключи

### ✅ Проверяйте перед коммитом:
```bash
git status
```
Убедитесь, что в списке НЕТ `.env` файла!

### 🔐 Если случайно закоммитили секреты:
```bash
# 1. Удалите файл из Git (но оставьте локально)
git rm --cached backend/.env

# 2. Закоммитьте удаление
git commit -m "Remove .env from git"

# 3. Отправьте на GitHub
git push

# 4. ОБЯЗАТЕЛЬНО смените API ключи на iLovePDF!
```

---

## 🌿 Работа с ветками (для продвинутых)

### Создать новую ветку
```bash
git checkout -b feature/new-feature
```

### Переключиться на другую ветку
```bash
git checkout main
```

### Слить ветку в main
```bash
git checkout main
git merge feature/new-feature
```

---

## 📥 Скачать изменения с GitHub

Если работаете с нескольких компьютеров:

```bash
# Скачать изменения
git pull
```

---

## 🆘 Если что-то пошло не так

### Отменить последний коммит (но оставить изменения)
```bash
git reset --soft HEAD~1
```

### Отменить последний коммит (и удалить изменения)
```bash
git reset --hard HEAD~1
```

### Вернуться к состоянию на GitHub
```bash
git fetch origin
git reset --hard origin/main
```

---

## 🎯 Следующие шаги

Теперь, когда проект на GitHub:

1. ✅ Зарегистрируйтесь на Railway → [RAILWAY_REGISTRATION.md](RAILWAY_REGISTRATION.md)
2. ✅ Задеплойте проект → [DEPLOY_RAILWAY.md](DEPLOY_RAILWAY.md)
3. ✅ Настройте GitHub Pages для frontend

---

## 📚 Полезные ссылки

- Ваш репозиторий: https://github.com/PtOlga/mega-pdf-compressor
- Git документация: https://git-scm.com/doc
- GitHub Guides: https://guides.github.com/

---

## 💡 Совет

Коммитьте часто, небольшими порциями. Лучше 10 маленьких коммитов, чем 1 огромный!

Хорошо:
- ✅ "Добавил CORS в backend"
- ✅ "Обновил README"
- ✅ "Исправил ошибку с загрузкой файлов"

Плохо:
- ❌ "Много изменений"
- ❌ "Обновление"
- ❌ "fix"

